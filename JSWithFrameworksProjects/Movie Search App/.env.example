# Environment Variables for Movie Search App
# Add this to your .env.local file (React/Vite)
# or .env file (for other frameworks)

# OMDB API Key - Get yours from http://www.omdbapi.com/apikey.aspx
VITE_OMDB_API_KEY=your_api_key_here

# Note: VITE_ prefix is required for Vite to expose the variable to the browser
# For React (Create React App), use REACT_APP_ prefix instead:
# REACT_APP_OMDB_API_KEY=your_api_key_here